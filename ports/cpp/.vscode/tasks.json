{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Test",
            "type": "shell",
            "command": [
                "make",
                "antlr4-c3-test"
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ]
}