{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build All",
            "type": "shell",
            "command": [
                "make",
                "all"
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ]
}